info:
  name: album-list-scratch
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/graphql"
  body:
    type: json
    data: |-
      {
        "query": "query AlbumListScratch($input: AlbumListInput!) { albumList(input: $input) }",
        "variables": {
          "input": {
            "prefix": "",
            "useScratchDir": true
          }
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns the scratch-only album entry", function () {
          const body = res.getBody();
          const json = typeof body === "string" ? JSON.parse(body) : body;

          expect(res.getStatus()).to.eql(200);
          expect(json).to.not.have.property("errors");
          expect(json).to.have.nested.property("data.albumList").that.includes(
            bru.getEnvVar("scratchAlbumEntry")
          );
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
